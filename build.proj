<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <SolutionFile>WebApp01.sln</SolutionFile>
  </PropertyGroup>

  <Target Name="Build">
    <MSBuild Projects="$(SolutionFile)" Targets="Build"  />
  </Target>
</Project>